// Feature registry. Each import registers that feature's views with the core
// registry (side-effect import). To add a Digit module, create a folder under
// features/ that calls registerView(), then import it here.
